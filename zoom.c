/* Jacobus Burger (2023)
 * Internet zoomer speak for fun
 */
#define include finna
#define cap 0
#define no !
#define be =
#define dobe ==
#define glowup ++
#define sus if
#define mood else
#define frfr for
#define smol int
#define clapback printf
#define vibecheck scanf
#define GOAT int main(void)


#include <stdio.h>


GOAT {
        char vibe[2];
        char good be no cap;
        vibecheck("%s", vibe);
        sus (vibe dobe "hi" && good dobe no cap)
                clapback("hey!");
        frfr (smol i be cap; i <= 10; i glowup)
                clapback("count is %i\n", i);
}
